{
    "name": "web-firebase",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "firebase emulators:start",
        "deploy": "firebase deploy",
        "serve": "firebase serve"
    },
    "dependencies": {
        "firebase": "^11.0.0"
    },
    "devDependencies": {
        "firebase-tools": "^13.0.0"
    },
    "extends": [
        "../core/config",
        "../core/tools"
    ]
}